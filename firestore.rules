// read: get, list / write: update, create, delete
// the second rule overwrites the first rule

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, create: if true;
      allow update, delete: if false;
    }
  }
}